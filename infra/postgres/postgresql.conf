# Minimal overrides required for Debezium's logical decoding (pgoutput plugin).
# Mounted over the default postgresql.conf via docker-entrypoint + `-c config_file=`.
listen_addresses = '*'
wal_level = logical
max_wal_senders = 4
max_replication_slots = 4

This continues ticket 0006 on the Wayfinder map for /home/vraj/Programming/flashseat — a previous opencode session on this exact ticket ran out partway through and never finished. Read /home/vraj/Programming/flashseat/docs/wayfinder/tickets/0006-drizzle-migrations-vs-init-sql.md for the original spec, and /home/vraj/Programming/flashseat/docs/wayfinder/map.md's Notes for standing project rules.

What the previous session already did (don't redo): confirmed `npx drizzle-kit generate` works (config auto-loads `.env` fine) and picked Option A (generate a baseline migration, since CLAUDE.md documents `db:generate`/`db:migrate` as expected-working commands for this portfolio project — do not remove the tooling). It ran `npx drizzle-kit generate` and it produced `backend/src/infrastructure/database/migrations/0000_solid_proemial_gods.sql` plus `backend/src/infrastructure/database/migrations/meta/` (0000_snapshot.json, _journal.json) — these files currently exist on disk, untracked in git, and are yours to keep/amend.

**The critical unfinished finding**: the generated migration's own output showed `0 indexes` for every table, but `infra/postgres/init.sql` actually has 2 real indexes (check init.sql directly — likely on `seats(event_id, status)` given that's the natural lookup index for the booking flow, but verify by reading the file, don't assume). This is real schema drift the Drizzle TS schema files are missing — the Drizzle `pgTable` schema definitions under `backend/src/infrastructure/database/schema/` never declared those indexes as Drizzle-managed indexes, so Drizzle doesn't know about them and would never generate them. This needs to be fixed BEFORE finalizing the baseline: add the missing index declarations to the Drizzle schema (Drizzle's `pgTable` third-argument callback supports `index()`/`uniqueIndex()` — check the Drizzle ORM version installed in package.json for the right API shape), regenerate the baseline migration so it actually matches init.sql completely (including these indexes), then confirm zero drift remains between the regenerated migration's DDL and init.sql.

Also confirmed by the previous session: the docker-compose Postgres (localhost:5434) has real data — 71 seats, 63 bookings — and a kind cluster is also up right now. **Do not drop or truncate anything.** The whole point of "Option A" here is reconciling migration history with an already-existing, already-populated database, not recreating it.

Once the migration is complete and drift-free:
1. Mark it as already-applied against both the docker-compose Postgres and (if still up) the kind cluster's Postgres, without actually re-running the CREATE TABLE statements (which would fail — the tables already exist). Drizzle-kit has a mechanism for this — check `npx drizzle-kit --help` and any migrate/push subcommands' options for an "already applied" / baseline-marking flow (some versions call this differently — investigate the actually-installed version rather than assuming a flag name). If the installed version genuinely has no such mechanism, the pragmatic alternative is manually inserting the appropriate row into Drizzle's internal migrations-tracking table (check what table/schema drizzle-kit's migrator expects — usually `drizzle.__drizzle_migrations` or similar) so a future `db:migrate` sees this baseline as already-run and no-ops cleanly.
2. Verify: run `npm run db:migrate` (or whatever the finalized command is) against the docker-compose Postgres and confirm it completes with "nothing to do" / no errors — NOT "relation already exists". This is the actual proof the ticket needs.
3. Check backend/package.json's `db:generate`/`db:migrate` scripts still make sense as-is, or need a comment/README note about the baseline-marking step for a future contributor who clones this repo fresh (a fresh clone + fresh `docker compose up` + `db:migrate` should also work cleanly against a brand-new empty database — think through both cases: existing populated DB from before this fix, and a fresh DB from scratch, both need to end up correct).
4. Update any doc that references these commands if genuinely stale (check README.md, k8s/README.md, CLAUDE.md) — only if actually stale, don't rewrite unrelated content.

Hard rules, non-negotiable, same as before:
- Do NOT run any git commands (add/commit/push/etc). A previous automated dispatch on this exact project committed unauthorized changes as the user (commit ab47712) — never repeat that.
- Do NOT touch router-core/, lb-proxy/, or k8s/ manifests (k8s/README.md docs are fine if stale).
- Do NOT touch backend/src/config/env.ts, backend/src/index.ts, backend/src/shared/middleware/rateLimiter.ts, or backend/src/modules/booking/ — other concurrent work has been active in those files tonight.
- Do NOT drop/truncate any table in either the docker-compose Postgres or the kind cluster's Postgres — both have real data from tonight's testing that must survive.

When done, write a full plain-text report to /home/vraj/Programming/flashseat/docs/wayfinder/context/0006-drizzle-migrations-report.md: the index-drift fix, the baseline-marking mechanism you used and why, and your verification results for both the "existing populated DB" and "fresh DB" scenarios (fresh DB can be verified conceptually/by reading the migration+config rather than literally spinning up a third Postgres, if that's more time-efficient — use your judgment).

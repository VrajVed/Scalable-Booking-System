You are resolving a ticket on a Wayfinder map for the repo at /home/vraj/Programming/flashseat ("AI-Powered Scalable Booking System"). Read /home/vraj/Programming/flashseat/docs/wayfinder/tickets/0007-compose-connector-credential-drift.md first — that is your full spec. Skim /home/vraj/Programming/flashseat/docs/wayfinder/map.md's Notes for standing project rules.

Context: a prior audit tonight fixed this exact bug class in the k8s deployment path (k8s/20-kafka-connect.yaml) — the Debezium connector's config JSON hardcoded Postgres credentials instead of reading the Secret everything else uses, so a credential rotation would silently break CDC. The fix there: templated the connector JSON with placeholders (__DB_USER__/__DB_PASSWORD__/__DB_NAME__), sourced the real values into the init container's env via the same secretKeyRef the Postgres StatefulSet uses, and had the registration script `sed`-substitute them before POSTing — with escaping hardened against passwords containing `/`, `&`, or `\` (a naive sed substitution breaks on those characters; the k8s fix uses `@` as the sed delimiter plus an explicit escape helper — read k8s/20-kafka-connect.yaml's register-connector.sh section, specifically the escape_for_sed-equivalent logic, to see the exact pattern to mirror).

This ticket asks you to apply the equivalent fix to the docker-compose path, which was never given it:
- infra/connector/postgres-connector.json currently hardcodes database.user/database.password/database.dbname to booking_system/booking_system/booking_system literally.
- infra/connector/register-connector.sh POSTs that file verbatim via `-d @/connector/postgres-connector.json`.
- infra/docker-compose.yml's connect-init container already has POSTGRES_USER/POSTGRES_PASSWORD/POSTGRES_DB available via infra/.env (check how the postgres service itself gets these — same source should work for connect-init; you may need to add env vars to the connect-init service definition if they aren't already passed through).

Implement: template the JSON (same placeholder style as the k8s fix, or your own consistent naming — just be consistent with the k8s side's approach so a reader recognizes the pattern), substitute real values from environment variables into register-connector.sh with the same escaping care, wire the needed POSTGRES_USER/POSTGRES_PASSWORD/POSTGRES_DB into the connect-init service's environment in docker-compose.yml if not already present.

Live verification (this is the important part — don't just trust that the script looks right):
1. `cd infra && docker compose down` is NOT allowed if it would destroy other running work — check first whether this compose stack is the only thing using these containers, and prefer `docker compose up -d --force-recreate connect-init` or similar to re-trigger just the registration step, rather than tearing down the whole stack. If you do need a fuller restart, that's fine as long as you're not destroying Postgres/Kafka data anyone might care about — this project's own docs describe this compose stack as freely disposable local dev infra, so recreating containers (not volumes) is fine, just avoid `docker compose down -v` (which would drop volumes/data).
2. Confirm the connector registers successfully (HTTP 201, or 409 if already registered) with the substituted (not literal placeholder) values — `curl http://localhost:8084/connectors/booking-system-connector/config` (check the actual host port from infra/.env.example) should show the real resolved credentials, not `__DB_USER__` placeholder text.
3. Confirm CDC actually still works after your change: make a real change to a seat row (UPDATE via psql, or drive it through the actual backend if it's running) and confirm the Kafka topic `booking-system.public.seats` receives the change event — same proof standard as tonight's earlier end-to-end validation, not just "registration returned 201."
4. Optionally (only if time permits and it's low-risk): temporarily change POSTGRES_PASSWORD in a copy of infra/.env, bring the stack up, confirm the connector picks up the new password correctly — this is the actual scenario the bug protects against. Revert afterward. Skip this step if it risks disrupting other things running on the machine; the registration+CDC-still-works check above is the core requirement.

Hard rules, non-negotiable:
- Do NOT run any git commands (add/commit/push/etc) under any circumstances. A previous automated dispatch on this exact project committed unauthorized changes as the user (commit ab47712) — never repeat that, not even "git status" out of habit.
- Do NOT touch router-core/, lb-proxy/, k8s/, or anything under backend/src/ — out of scope, and other concurrent work may be active in backend/src/ right now.
- Do NOT run `docker compose down -v`, `docker system prune`, or anything that destroys volumes/data. Do NOT touch or restart a kind cluster if one happens to be running (`kind get clusters`) — that's a separate deployment path from docker-compose, leave it alone entirely.
- The docker-compose stack under infra/ is meant to be freely recreatable local dev infra (per this project's own docs) — recreating containers is fine, destroying volumes is not.

When done, write a full plain-text report to /home/vraj/Programming/flashseat/docs/wayfinder/context/0007-compose-connector-creds-report.md covering: exactly what you changed (file:line), and your live verification results for each of steps 1-3 above (step 4 optional, note if skipped and why).

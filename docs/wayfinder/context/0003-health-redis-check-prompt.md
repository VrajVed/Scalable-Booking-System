You are resolving a ticket on a Wayfinder map for the repo at /home/vraj/Programming/flashseat ("AI-Powered Scalable Booking System"). Read /home/vraj/Programming/flashseat/docs/wayfinder/tickets/0003-health-endpoint-redis-check.md first — that is your full spec. Skim /home/vraj/Programming/flashseat/docs/wayfinder/map.md's Notes for standing project rules.

Context: backend's /health endpoint (defined in backend/src/index.ts) reports "ok" regardless of Redis state. If Redis is unreachable at boot, the process neither crashes nor hangs — app.listen() succeeds and /health passes — but every subsequent real request then hangs forever inside the rate-limiter's preHandler (backend/src/shared/middleware/rateLimiter.ts), because it needs Redis and never times out. A k8s readinessProbe hitting /health would never catch this and would keep routing traffic to a pod that hangs on every request. There's a pre-existing `it.todo` in backend/test/shared/rateLimiter.test.ts documenting exactly this gap as a known, deliberately-unfixed design decision — your fix may finally let that todo be resolved, check its exact wording once you're in the file.

Implement:
1. Add a real Redis reachability check to /health — a fast `PING` with a short timeout (don't block on a hung connection), not a blocking wait. Check backend/src/config/redis.ts for the existing `redisConnection` (ioredis client) export and use it directly rather than creating a second connection.
2. Check k8s/40-backend.yaml's current readinessProbe configuration (path, expectations, timeoutSeconds) before changing /health's response shape — you must not break what that probe currently expects. Decide whether /health should return a non-200 status (e.g. 503) when Redis is down, matching what a k8s readinessProbe needs to correctly mark the pod NotReady — implement that.
3. This directly unblocks the rateLimiter.test.ts `it.todo` — read it, decide whether your fix lets it become a real assertion (e.g. "when Redis is down, /health reports failure" rather than testing the previously-unfixable hang directly), and if so, un-todo it. If you resolve the todo, say so explicitly in your report — this is worth getting right since the ticket exists partly because of that todo.
4. Add a new test proving a Redis outage is actually caught: mock/simulate Redis being unreachable (check how other tests in backend/test/ that need infra failures do this — there may be a pattern already, e.g. temporarily disconnecting the shared redisConnection) and assert /health now returns the failure status you implemented, with a tight timeout on the test itself (not a real multi-second hang).

Before you start: run `cd backend && npm run build && npm test` for a baseline (infra needs to be up — check `docker compose ps` in infra/, run `docker compose up -d` there if not running). Record exact counts. After: rerun, must stay green with your new test(s), no regressions, no weakened assertions.

Live verification if a kind cluster is up (`kind get clusters` may show `booking-system`): scale Redis to 0 replicas temporarily (`kubectl scale deployment/redis --replicas=0`), confirm /health now reports failure and the readinessProbe actually marks backend pods NotReady (`kubectl get pods -l app=booking-backend` should show `0/1` or similar), then scale Redis back to 1 and confirm recovery. This is a non-destructive, easily-reversible scaling operation, not deleting anything — safe to do. If this risks disrupting other concurrent work on the same cluster, skip it and note why; unit test proof is the core requirement.

Hard rules, non-negotiable:
- Do NOT run any git commands (add/commit/push/etc) under any circumstances. A previous automated dispatch on this exact project committed unauthorized changes as the user (commit ab47712) — never repeat that, not even "git status" out of habit.
- Do NOT touch lb-proxy/, router-core/, or k8s manifests other than reading k8s/40-backend.yaml for context — out of scope to edit it unless the readinessProbe path/config genuinely needs to change to match your new /health contract, in which case a minimal edit is fine, just note it clearly.
- Another concurrent dispatch (ticket 0004) may be simultaneously editing index.ts's shutdown-handling code around the same time as you — if you hit an Edit failure because the file changed under you, just re-read the current content and adapt, don't assume corruption. Keep your index.ts changes minimal and surgical (the /health route body) to reduce collision surface.
- Do NOT scale down postgres, kafka, kafka-connect, or booking-backend itself — only redis is safe to scale for your verification step.

When done, write a full plain-text report to /home/vraj/Programming/flashseat/docs/wayfinder/context/0003-health-redis-check-report.md: exactly what you changed (file:line), whether the pre-existing it.todo got resolved and how, before/after test counts, and live verification result if you did it.

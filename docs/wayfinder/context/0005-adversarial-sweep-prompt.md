You are resolving a research ticket on a Wayfinder map for the repo at /home/vraj/Programming/flashseat ("AI-Powered Scalable Booking System"). Read /home/vraj/Programming/flashseat/docs/wayfinder/tickets/0005-second-adversarial-sweep.md first — that is your full spec, containing the exact areas to investigate. Also skim /home/vraj/Programming/flashseat/docs/wayfinder/map.md for the Notes section (standing project rules) and Decisions-so-far (what's already been fixed tonight, so you don't re-flag it).

Your job has four investigation areas, all detailed in the ticket file. Work through each one, and for each: report what you checked, what you found, and whether you fixed it (only fix if it's small and unambiguous — a one-line escaping bug, a missing `set -euo pipefail`, a genuinely weak test assertion. If a finding is bigger or requires a design decision, write it up clearly in your report instead of fixing it blind).

The four areas (full detail in the ticket file, summarized here):
1. Three-way schema check: Drizzle schema TS files vs infra/postgres/init.sql vs any actual generated migration files under backend/ (check backend/drizzle.config.ts for the migrations output path — likely backend/drizzle/ or similar). Look for drift that would make a future `npm run db:generate` produce a migration conflicting with what's already live.
2. Read infra/connector/register-connector.sh and any other shell scripts under infra/ adversarially for unescaped substitution, unquoted variables, missing strict-mode flags — the same bug class a prior audit pass already found and fixed in the analogous k8s Job script.
3. Follow k8s/README.md's instructions as literally written and check whether they'd actually work end-to-end for someone starting fresh, given everything that changed in tonight's k8s audit pass (HPA maxReplicas capped to 3, connector ConfigMap now templated with credential substitution instead of a static file, securityContext added to backend/lb-proxy, lb-proxy image rebuilt with graceful shutdown). Does the README still match what's actually in the k8s/ manifests, or is anything now stale/undocumented?
4. Read every file under backend/test/ for weak assertions — code that runs but doesn't actually assert the behavior it claims (e.g. asserting a promise resolved without checking what it resolved to, or catching an error and only checking "something was thrown" without checking it's the right error type/message). This is explicitly the bug class this project's standing testing philosophy cares most about.

For area 4 specifically: if you find a weak test, you may strengthen its assertion (make it actually prove what it claims to prove) — that is fixing the test to be more correct, which is different from weakening a test to make it pass (forbidden). If strengthening an assertion causes the test to newly fail, that means the weak assertion was hiding a real bug — investigate and report that as a finding, don't just revert the strengthening to make it pass again.

Hard rules, non-negotiable:
- Do NOT run any git commands (add/commit/push/etc) under any circumstances. A previous automated dispatch on this exact project violated this and committed unauthorized changes as the user (commit ab47712, still sitting in git history because of it) — do not repeat that, not even "git status" out of habit.
- Do NOT touch anything under router-core/ (must stay a clean vendored library, per project constraint).
- Stay out of active-development files in backend/src/modules/booking/ and backend/src/index.ts if you notice they're changing while you work (another concurrent agent is implementing a hold-expiry feature there right now) — if you find something worth reporting in those files, report it, don't edit it, to avoid stepping on that concurrent work.
- If a live kind cluster is running (`kind get clusters`), you may use read-only `kubectl get/describe` to help verify area 3, but do not delete/restart/apply anything to it.

When done, write a full plain-text report to /tmp/claude-1000/-home-vraj-Programming-flashseat/f8cdc317-7e94-444a-9cbb-34a14da52b93/scratchpad/opencode-ticket-0005-report.txt covering all four areas: what was checked, what was found (bug or clean), what was fixed vs. just reported, and file:line references throughout.

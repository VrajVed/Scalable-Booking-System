You are resolving a ticket on a Wayfinder map for the repo at /home/vraj/Programming/flashseat ("AI-Powered Scalable Booking System"). Read /home/vraj/Programming/flashseat/docs/wayfinder/tickets/0002-rate-limiter-trust-proxy.md first — that is your full spec. Also skim /home/vraj/Programming/flashseat/docs/wayfinder/map.md's Notes section for standing project rules.

Context in one paragraph: backend/src/shared/middleware/rateLimiter.ts keys rate-limiting on `request.headers["x-forwarded-for"] ?? request.ip`, with no Fastify `trustProxy` configured. lb-proxy (the Rust reverse proxy in front of the backend, source at lb-proxy/src/proxy.rs) forwards headers verbatim to backends and never sets/appends X-Forwarded-For with the real client IP. So right now any client can rotate the X-Forwarded-For header per request to dodge rate limiting entirely, or set it to someone else's IP to get them rate-limited instead.

The topology is simple and worth stating plainly: in this deployment, lb-proxy is the ONLY hop between the internet-facing ingress and the backend — there is no chain of untrusted proxies to worry about, just one trusted edge (lb-proxy) and the backend behind it. That single-hop fact should drive your decision.

Your job: make and implement a real trust-boundary decision, per the ticket's two acceptable options:
1. (Preferred, matches ticket's stated preference) Fix it at the edge: have lb-proxy set or overwrite the X-Forwarded-For header with the actual peer IP of the inbound TCP connection before forwarding to a backend (standard reverse-proxy behavior — look at how proxy.rs currently builds/forwards the outbound request in its `handle()` function, find where headers get copied/stripped (there's already hop-by-hop header stripping — `strip_hop_by_hop()` — mirror that code's shape for where you add this), and set X-Forwarded-For there using the actual accepted-connection's remote address, not any client-supplied value). Then on the backend side, configure Fastify's `trustProxy` (check Fastify's docs/types for the right shape given a single trusted hop) so it trusts exactly one hop, and rateLimiter.ts can safely rely on request.ip once trustProxy is configured (Fastify computes request.ip correctly from X-Forwarded-For only when trustProxy says to).
2. (Acceptable alternative, ticket allows this if you state the reasoning) Backend-only: ignore X-Forwarded-For entirely, key on the raw peer IP as lb-proxy's own TCP connection info would show it — but note this only works correctly if lb-proxy is a true L4/L7 proxy your backend sees as the sole peer, which given this topology it is, so `request.ip` without any trustProxy dance may already be exactly right today since Fastify listens for direct connections from lb-proxy. Investigate which is actually true architecturally before picking this path — don't assume, check how the backend's HTTP server actually sees the connection (does it terminate the raw TCP connection lb-proxy makes, or is there yet another hop you're missing, e.g. through the k8s Service?).

Whichever you pick: implement it fully (code, not just a comment), add a test proving a spoofed X-Forwarded-For header from a client can no longer bypass or misdirect the rate limit, and write a clear resolution explaining which option you picked and why (this gets recorded on the map — be explicit, a vague "fixed it" is not acceptable here since this ticket exists specifically because the prior decision was silent/implicit).

Before changing lb-proxy: run `cd lb-proxy && cargo test` for a baseline (record pass count). After: rerun, must stay green plus your new test, and also run `cargo clippy --all-targets` and keep it at 0 warnings (it's currently 0). Before changing the backend: run `cd backend && npm run build && npm test` for baseline (infra needs to be up: check `docker compose ps` in infra/, run `docker compose up -d` there if not running). After: rerun, must stay green with your new test, no regressions, no weakened assertions.

Hard rules, non-negotiable:
- Do NOT run any git commands (add/commit/push/etc) under any circumstances. A previous automated dispatch on this exact project violated this and committed unauthorized changes as the user (commit ab47712, still sitting in git history because of it) — do not repeat that, not even "git status" out of habit.
- Do NOT touch router-core/ (must stay a clean vendored library).
- Do NOT touch backend/src/modules/booking/ or backend/src/index.ts beyond what's strictly needed for a trustProxy config line — a concurrent agent is actively working on a hold-expiry feature in that area; if you need to touch index.ts, make a minimal, surgical, clearly-scoped edit (e.g. adding one `trustProxy` option to the Fastify constructor call) and nothing else, to minimize collision risk.
- If a live kind cluster is up (`kind get clusters` may show `booking-system`), you may rebuild+redeploy lb-proxy and/or backend images to verify live, but this is optional — unit/integration test proof plus a clearly reasoned decision is the core requirement. Do not delete/tear down the cluster.

When done, write a full plain-text report to /tmp/claude-1000/-home-vraj-Programming-flashseat/f8cdc317-7e94-444a-9cbb-34a14da52b93/scratchpad/opencode-ticket-0002-report.txt: which option you chose and why, exactly what you changed (file:line), test results before/after for both lb-proxy and backend, and live verification if you did it.

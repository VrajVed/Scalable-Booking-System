You are resolving a ticket on a Wayfinder map for the repo at /home/vraj/Programming/flashseat ("AI-Powered Scalable Booking System"). Read /home/vraj/Programming/flashseat/docs/wayfinder/tickets/0004-backend-graceful-shutdown.md first — that is your full spec. Skim /home/vraj/Programming/flashseat/docs/wayfinder/map.md's Notes for standing project rules.

Context: backend/src/index.ts has no SIGTERM/SIGINT handler. lb-proxy already got the equivalent fix tonight (ticket 0002 also just landed changes there for an unrelated reason — X-Forwarded-For handling — don't worry about that, it's done and stable): hyper_util::GracefulShutdown with a 25s drain budget under k8s's 30s default terminationGracePeriodSeconds, verified with a test that actually proves the shutdown blocks on an in-flight request (not just that the connection happens to survive). Mirror that rigor here, adapted to Node/Fastify.

Two other tickets already landed in backend/src/index.ts tonight and are now stable (not being actively edited): ticket 0001 (hold-expiry) added `startHoldExpiryWorker()` on boot and exports a `stopHoldExpiryWorker()` from backend/src/infrastructure/queue/hold-expiry.worker.ts specifically so a SIGTERM handler could call it — that's you, now. Ticket 0002 (rate limiter trust boundary) added a `trustProxy` option to the Fastify constructor call — unrelated to your work, just be aware index.ts has more in it now than a bare-bones app.

What already exists for you to call during shutdown (read these files to confirm current signatures before using them):
- `disconnectProducer()` — backend/src/infrastructure/kafka/producer.ts
- CDC consumer disconnect — check backend/src/infrastructure/kafka/cdc-consumer.ts for how startCdcConsumer's returned consumer (or an exported stop function) gets torn down; use whatever the existing shape actually offers.
- `closeDb()` — backend/src/infrastructure/database/db.ts
- `stopHoldExpiryWorker()` — backend/src/infrastructure/queue/hold-expiry.worker.ts (waits for any in-flight job to finish per BullMQ's Worker#close semantics, and also closes the queue's Redis connection)
- Fastify's own `app.close()` — drains in-flight HTTP requests before resolving.

Implement:
1. A SIGTERM/SIGINT handler in index.ts that, in a sensible order (stop accepting new work first, then drain/close dependencies): calls `app.close()` (stops accepting new connections, waits for in-flight requests), then closes Kafka producer/consumer, hold-expiry worker, and the DB pool — using the functions above. Don't reinvent teardown logic that already exists.
2. Bound the whole thing with a timeout (mirror lb-proxy's reasoning: something under k8s's default 30s terminationGracePeriodSeconds — e.g. 25s) so a stuck connection or hung external call can't block shutdown forever; force-exit after the timeout if graceful drain hasn't finished.
3. A test proving this actually works — not just "the function runs without throwing." The lb-proxy test achieved this by temporarily breaking the fix, watching the test correctly fail, then restoring it; use the same discipline here. A `tsx --test` process-level test is harder for this than lb-proxy's Rust integration test (you can't easily fork a full second Node process and send it a real SIGTERM from within the same test runner in the exact way, though you can — check if `node:child_process` spawning the actual compiled/dev server and sending it a real SIGTERM, then asserting on its exit behavior and stdout, is feasible within this test suite's existing patterns; look at how other integration-style tests in backend/test/ are structured for a precedent). If a full process-spawn test is impractical given time constraints, a more targeted test that proves the shutdown function actually calls each teardown step in order and actually waits for an in-flight request/job before resolving (using the same call-order/blocking-proof technique, just at the function level rather than a full process spawn) is an acceptable fallback — but don't settle for "it compiles and doesn't throw" as your bar.
4. Live verification if a kind cluster is up (`kind get clusters` may show `booking-system`): rebuild the backend image, reload it, `kubectl rollout restart statefulset/booking-backend` while a slow-ish request is in flight (or fire a request right as the rollout starts), confirm no dropped connections/500s during the rollout and zero unexpected restarts (`kubectl get pods -l app=booking-backend` restart counts should stay 0). This is optional if it's too time-consuming or risks disrupting other concurrent work — unit/integration test proof is the core requirement.

Before you start: run `cd backend && npm run build && npm test` for a baseline (infra needs to be up — check `docker compose ps` in infra/, run `docker compose up -d` there if not running). Record exact counts. After: rerun, must stay green with your new test(s), no regressions, no weakened assertions.

Hard rules, non-negotiable:
- Do NOT run any git commands (add/commit/push/etc) under any circumstances. A previous automated dispatch on this exact project committed unauthorized changes as the user (commit ab47712) — never repeat that, not even "git status" out of habit.
- Do NOT touch lb-proxy/, router-core/, or k8s/ manifests — out of scope.
- Another concurrent dispatch (ticket 0003) may be simultaneously editing index.ts's health-check route around the same time as you — if you hit an Edit failure because the file changed under you, just re-read the current content and adapt your edit, don't assume corruption. Keep your index.ts changes minimal and surgical (the signal handler registration + its body) to reduce collision surface.

When done, write a full plain-text report to /home/vraj/Programming/flashseat/docs/wayfinder/context/0004-backend-shutdown-report.md: what you built, how your test(s) actually prove the behavior (not just exercise it), before/after test counts, and live verification result if you did it.

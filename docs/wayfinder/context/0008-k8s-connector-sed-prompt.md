You are resolving a ticket on a Wayfinder map for the repo at /home/vraj/Programming/flashseat ("AI-Powered Scalable Booking System"). Read /home/vraj/Programming/flashseat/docs/wayfinder/tickets/0008-k8s-connector-sed-backslash-bug.md first — that is your full spec. Skim /home/vraj/Programming/flashseat/docs/wayfinder/map.md's Notes for standing project rules.

One-paragraph context: k8s/20-kafka-connect.yaml has a register-connector.sh (embedded in a ConfigMap) that substitutes real Postgres credentials into a Debezium connector JSON template using sed with an escape_for_sed helper. A sibling ticket (0007) just fixed the equivalent bug in the docker-compose path's script (infra/connector/register-connector.sh) after discovering — by testing inside the actual curlimages/curl:8.10.1 image both paths run in — that busybox sed's bracket-class escaping does not correctly handle a literal backslash in a password, producing invalid JSON. Read infra/connector/register-connector.sh's current (already-fixed) awk-based substitution block in full — that's your reference implementation, already proven safe for @, &, \, and " by live testing in that exact image.

Your job: port that same awk approach (or an equally rigorous alternative you can prove safe the same way) into k8s/20-kafka-connect.yaml's embedded register-connector.sh script (it's inside a ConfigMap's `data:` block, so you're editing YAML-embedded shell, not a standalone file — be careful with YAML indentation/escaping when editing). The k8s script has its own postgres-connector.json template embedded in the same ConfigMap (or nearby — check the full file) with the __DB_USER__/__DB_PASSWORD__/__DB_NAME__ placeholders already established by an earlier audit pass tonight; keep those placeholder names, just fix the substitution mechanism.

Verify the same way ticket 0007 did — this is the important part, don't just eyeball that it looks right:
1. Test the substitution logic standalone inside the actual curlimages/curl:8.10.1 image (docker run -it curlimages/curl:8.10.1 sh, or exec into a running container using that image if one exists) with a hostile test value containing @, &, \, and " all at once in the password field — confirm the resulting JSON is valid (parseable) and the decoded value round-trips byte-for-byte.
2. If a live kind cluster is up (`kind get clusters` may show `booking-system`), verify end-to-end: this requires recreating the connect-init Job (Job pod templates are immutable once created — you'll need `kubectl delete job connect-init` then reapply k8s/20-kafka-connect.yaml, or an equivalent approach). Confirm the connector registers with resolved (not placeholder) values and that CDC events still flow for a real seat UPDATE (same proof standard used throughout tonight — an actual Kafka message showing the actual before/after values, not just "registration returned 201").
   - IMPORTANT: deleting and recreating the connect-init Job is a real k8s operation but is NOT the kind of "destructive" action to avoid — it's a stateless registration Job, not stateful data (Postgres/Kafka data is untouched by this). This is explicitly fine. Do NOT delete/restart anything else (postgres, kafka StatefulSets, or any PVC) — only the connect-init Job itself.
   - If the live-cluster verification would be disruptive to other concurrent work or is too time-consuming, the image-level test in step 1 is the core requirement — use your judgment, note what you skipped and why.

Hard rules, non-negotiable:
- Do NOT run any git commands (add/commit/push/etc) under any circumstances. A previous automated dispatch on this exact project committed unauthorized changes as the user (commit ab47712) — never repeat that, not even "git status" out of habit.
- Do NOT touch infra/ (already fixed by ticket 0007) or anything outside k8s/20-kafka-connect.yaml.
- Do NOT delete/restart the postgres, kafka, kafka-connect, redis, booking-backend, or lb-proxy workloads, and do NOT touch any PVC — only the connect-init Job is fair game to delete+recreate.
- Do NOT touch router-core/, lb-proxy/, or backend/src/ — out of scope.

When done, write a full plain-text report to /home/vraj/Programming/flashseat/docs/wayfinder/context/0008-k8s-connector-sed-report.md: exactly what you changed, your image-level hostile-character test result, and live-cluster verification result if you did it (or why you skipped it).
